[core]
	engine = mysql
	# plan_file = sqitch.plan
	# top_dir = .
# [engine "mysql"]
	# target = db:mysql:
	# registry = sqitch
	# client = mysql
[target "localdb"]
	uri = db:mysql://root:password@localhost:3306/sqitch-cicd-test
